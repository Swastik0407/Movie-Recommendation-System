# Movie-Recommendation-System
A context-based Movie Recommendation System built using a dataset of 5,000 movies, with movie data converted into embeddings and stored in a Vector Database for semantic search. Developed an interactive Streamlit web application that understands user queries and recommends relevant movies based on contextual similarity.
